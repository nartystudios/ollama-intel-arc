name: Build and Push ollama-sycl

on:
  push:
    branches: [main]
    paths:
      - 'ollama-sycl/**'
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
    inputs:
      ollama_version:
        description: 'Ollama version (leave empty for latest)'
        required: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/ollama-sycl
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get latest Ollama version
        id: ollama
        run: |
          VERSION="${{ github.event.inputs.ollama_version }}"
          if [ -z "$VERSION" ]; then
            VERSION=$(curl -s https://api.github.com/repos/ollama/ollama/releases/latest | jq -r .tag_name | sed 's/v//')
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building Ollama v$VERSION"

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./ollama-sycl
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.ollama.outputs.version }}
          build-args: |
            OLLAMA_VERSION=${{ steps.ollama.outputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
